<%- DashboardConfig.widgets.each_with_index do |widget_config, index| -%>
<%= render :partial => "widgets_#{widget_config[:type]}", :locals => {:config => widget_config, :index => index} %>
<%- end -%>